spring.application.name=config-server

server.port=8888

#To define the uri of the registry 
eureka.client.service-url.defaultZone=${eureka-endpoint}

spring.cloud.config.server.native.search-locations=${config-path}
spring.profiles.active=native


#Take configurations from remote git repos
#spring.cloud.config.server.git.uri=${git-uri}
#spring.cloud.config.server.git.username=${git-username}
#spring.cloud.config.server.git.password=${git-pass}